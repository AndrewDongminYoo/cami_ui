// 🐦 Flutter imports:
import 'package:flutter/material.dart';

// 이것들은 피그마 디자인의 뷰포트 값입니다.
// 이것들은 코드에서 UI를 반응적으로 만들기 위한 참조로 사용됩니다.
const double FIGMA_WIDTH = 393;
const double FIGMA_HEIGHT = 844;

const Size figmaFrameSize = Size(FIGMA_WIDTH, FIGMA_HEIGHT);
